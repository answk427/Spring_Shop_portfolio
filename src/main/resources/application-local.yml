spring:
  datasource:
    url: jdbc:mysql://localhost:3307/trade_db?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: asdf # 로컬 DB 비번
    driver-class-name: com.mysql.cj.jdbc.Driver

  data:
    redis:
      host: localhost
      port: 6379

cors:
  allowed-origins: http://localhost:3000

file:
  storage:
    type: local
  upload:
    dir: uploads
  display:
    prefix: "http://localhost:8080/display?filename="