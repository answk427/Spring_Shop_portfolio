spring:
  profiles:
    active: test

  datasource:
    url: jdbc:mysql://localhost:3306/study_trade?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: asdf
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: true

  logging:
    level:
      org:
        springframework:
          jdbc: debug

jwt:
  secret: imsi-secret-key-afwefw-70684-96928592834928349283492834928349283
  expiration: 3600000 #24시간 (밀리초)