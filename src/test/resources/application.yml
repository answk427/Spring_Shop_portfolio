spring:
  profiles:
    active: test

  datasource:
    url: jdbc:mysql://localhost:3306/study_trade?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: asdf
    driver-class-name: com.mysql.cj.jdbc.Driver

  redis:
    host: localhost
    port: 6379
    timeout: 2000
    password:
    database: 0
    lettuce:
      pool:
        max-active: 8
        max-idle: 8
        min-idle: 0

  jpa:
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: true

  logging:
    level:
      org:
        springframework:
          jdbc: debug

jwt:
  secret: imsi-secret-key-afwefw-70684-96928592834928349283492834928349283
  expiration: 900000       # 15분 (ms)
  refresh:
    expiration: 604800000    # 7일 (ms)