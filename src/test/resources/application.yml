spring:
  profiles:
    active: test

  datasource:
    url: jdbc:mysql://localhost:3306/study_trade?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: asdf
    driver-class-name: com.mysql.cj.jdbc.Driver

  redis:
    host: localhost
    port: 6379
    timeout: 2000
    password:
    database: 0
    lettuce:
      pool:
        max-active: 8
        max-idle: 8
        min-idle: 0

  jpa:
    hibernate:
      ddl-auto: validate
#    show-sql: true
    properties:
      hibernate:
        highlight_sql: true # 콘솔에서 SQL 키워드에 색상 부여 (가독성 UP)
        use_sql_comments: true # 어떤 로직 때문에 쿼리가 나갔는지 주석 표시

  flyway:
    enabled: true

  logging:
    level:
      root: info
      work.trade: debug             # 네 프로젝트 패키지 로그
      org.hibernate.SQL: debug      # SQL 쿼리 로그
      org.hibernate.type.descriptor.sql: trace # 쿼리 파라미터 로그 (Hibernate 5)
      org.hibernate.orm.jdbc.bind: trace      # 쿼리 파라미터 로그 (Hibernate 6)

jwt:
  secret: imsi-secret-key-afwefw-70684-96928592834928349283492834928349283
  expiration: 900000       # 15분 (ms)
  refresh:
    expiration: 604800000    # 7일 (ms)